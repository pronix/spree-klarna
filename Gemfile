source 'https://rubygems.org'

group :assets do
  gem 'coffee-rails'
  gem 'sass-rails'
end # fixes scss trails error

gem 'spree', '~> 2.0.0'

# provides basic authentication functionality for testing parts of your engine
gem 'spree_auth_devise', github: 'spree/spree_auth_devise', branch: '2-0-stable'

gemspec
